// Generated by dedot cli

import type { GenericChainViewFunctions, GenericViewFunction } from 'dedot/types';
import type { AccountId32Like } from 'dedot/codecs';
import type {
  AstarRuntimeRuntimeCallLike,
  AstarRuntimeProxyType,
  PalletAssetsAssetDetails,
  PalletAssetsAssetMetadata,
} from './types.js';

export interface ChainViewFunctions extends GenericChainViewFunctions {
  /**
   * Pallet `Proxy`'s view functions
   **/
  proxy: {
    /**
     * Check if a `RuntimeCall` is allowed for a given `ProxyType`.
     *
     * @param {AstarRuntimeRuntimeCallLike} call
     * @param {AstarRuntimeProxyType} proxyType
     **/
    checkPermissions: GenericViewFunction<
      (call: AstarRuntimeRuntimeCallLike, proxyType: AstarRuntimeProxyType) => Promise<boolean>
    >;

    /**
     * Check if one `ProxyType` is a subset of another `ProxyType`.
     *
     * @param {AstarRuntimeProxyType} toCheck
     * @param {AstarRuntimeProxyType} against
     **/
    isSuperset: GenericViewFunction<
      (toCheck: AstarRuntimeProxyType, against: AstarRuntimeProxyType) => Promise<boolean>
    >;

    /**
     * Generic pallet view function
     **/
    [name: string]: GenericViewFunction;
  };
  /**
   * Pallet `Assets`'s view functions
   **/
  assets: {
    /**
     * Provide the asset details for asset `id`.
     *
     * @param {bigint} id
     **/
    assetDetails: GenericViewFunction<(id: bigint) => Promise<PalletAssetsAssetDetails | undefined>>;

    /**
     * Provide the balance of `who` for asset `id`.
     *
     * @param {AccountId32Like} who
     * @param {bigint} id
     **/
    balanceOf: GenericViewFunction<(who: AccountId32Like, id: bigint) => Promise<bigint | undefined>>;

    /**
     * Provide the configured metadata for asset `id`.
     *
     * @param {bigint} id
     **/
    getMetadata: GenericViewFunction<(id: bigint) => Promise<PalletAssetsAssetMetadata | undefined>>;

    /**
     * Provide the configured reserves data for asset `id`.
     *
     * @param {bigint} id
     **/
    getReservesData: GenericViewFunction<(id: bigint) => Promise<Array<[]>>>;

    /**
     * Generic pallet view function
     **/
    [name: string]: GenericViewFunction;
  };
}
